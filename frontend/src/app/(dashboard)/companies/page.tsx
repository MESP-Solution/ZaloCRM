import { ModulePlaceholder } from "~features/app-shell/components/module-placeholder";

export default function CompaniesPage() {
  return (
    <ModulePlaceholder
      title="Companies"
      description="Account records, segments, contacts, and deal relationships."
    />
  );
}
