import { ModulePlaceholder } from "~features/app-shell/components/module-placeholder";

export default function DealsPage() {
  return (
    <ModulePlaceholder
      title="Deals"
      description="Sales opportunities, stages, values, forecast, and next actions."
    />
  );
}
